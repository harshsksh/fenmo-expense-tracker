import DashboardLoading from './loading';

export default function DashboardPage() {
  // For Phase 3, we simply render the loading skeleton as a placeholder.
  // In Phase 5, this will be replaced with actual data fetching and the ExpenseList/ExpenseForm components.
  return <DashboardLoading />;
}
